name: Docker Publish (DEPRECATED)

# DEPRECATED: This workflow has moved to the protocol repo where it belongs.
# See: https://github.com/disentangle-network/protocol/.github/workflows/docker-publish.yml
# Tag the protocol repo with v* to trigger image builds.
#
# This file is kept temporarily so CI doesn't break if referenced.
# Remove after protocol repo workflow is confirmed working.

on:
  workflow_dispatch:  # manual only, no automatic triggers

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "This workflow has moved to the protocol repo."
          echo "Push a v* tag to disentangle-network/protocol to build and publish images."
          exit 0
